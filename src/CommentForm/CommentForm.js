import React, { useState } from 'React';
import { SafeAreaView } from 'react-native'

export const CommentForm = ({ rating }) => {
  const [userRating, setUserRating] = useState(rating);

  return(
    <SafeAreaView>
      
    </SafeAreaView>
  )
}